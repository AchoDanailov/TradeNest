@model ProductViewModel

<div class="card h-100 shadow d-flex flex-row" style="max-height: 190px">
    <div class="card-img-container ms-2 mt-1"> 
        <img src="@Model.FrontImageUrl" class="card-img-left" loading="lazy"
             alt="@Model.Name" />
    </div>
    <div class="card-body d-flex flex-column justify-content-between p-3">
        <div>
            <h5 class="card-title mb-1">@Model.Name</h5>
            <h6 class="card-subtitle text-muted">@this.Model.CategoryName</h6>
        </div>
        <div class="mt-2 h5 text-success">
            €@this.Model.SellingPrice
        </div>
        <div class="d-flex justify-content-end gap-2 mt-2">
            <a asp-controller="Products" asp-action="Details" asp-route-id="@this.Model.Id"
               class="btn btn-outline-teal btn-sm">Details</a>
            @*TODO: Link this*@
            @* <form method="post" asp-controller="Order" action="Index"> *@
                @* <input type="hidden" name="productId" value="@this.Model.Id" /> *@
                <button type="submit" class="btn btn-teal btn-md">Order Now</button>
            @* </form> *@
        </div>
    </div>
</div>
